{
  "name": "declarative-gridstack",
  "version": "1.0.0",
  "description": "gridstack.js as React components, build interactive dashboards in minutes.",
  "main": "index.js",
  "author": "Shibi Suriya P",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:react": "lerna run dev --scope=@declarative-gridstack/react",
    "dev:vue": "lerna run dev --scope=@declarative-gridstack/vue",
    "build:gridstack": "lerna run build --scope=@declarative-gridstack/core",
    "build:gridstack:es6": "lerna run build:es6 --scope=gridstack"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^7.0.1"
  }
}
